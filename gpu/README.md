# GPU inference (planned / 规划中)

A cuBLAS-based GPU version of the inference engine will live here.
基于 cuBLAS 的 GPU 推理版本将放在这个目录。

Status: not implemented yet — see `../cpu/` for the working CPU implementation
(`run.cpp` FP32, `runq.cpp` int8), which this port will mirror.
目前尚未实现——可用的 CPU 实现见 `../cpu/`（`run.cpp` FP32、`runq.cpp` int8），GPU 版本将与其对应。
